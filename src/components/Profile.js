
const Profile = ({username}) => {
  return (
    <div>     
        <h1 className='welcome-header'>User: </h1>
        <h2 className='username-header'>{username}</h2>
    </div>

  )
}

export default Profile
